
name: Node.js CI

on:
  push:
    branches: [ main ]
  schedule:
    - cron: "1 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: '14'
    - uses: actions/checkout@v2
    - run: |
        git config user.name "nmhung2022"
        git config user.email "nmhung2022@gmail.com"
        git add .
        git commit -m "update txt"
        git push
