name: Node.js CI

on:
  push:
    branches: [ main ]
  schedule:
    - cron: "1 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: '14'
    - run: |
        git config --global user.name "nmhung2022"
        git config --global user.email "nmhung2022@gmail.com"
        git add .
        git commit -m "update README.md"|| echo "No changes to commit"
        git push
