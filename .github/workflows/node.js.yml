
name: Node.js CI

on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          git config user.name "nmhung2022"
          git config user.email "nmhung2022@github.com"
          git add a.txt
          git commit -m "update txt"
          git push
